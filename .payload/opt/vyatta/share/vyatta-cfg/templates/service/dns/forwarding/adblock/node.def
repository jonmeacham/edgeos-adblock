priority: 999 # Run after DNS forwarding is configured
help: Configure dnsmasq adblocking, redirect IP, allow rules, blocks, and sources

end:
    sudo _OFR_CONFIGURE=ok /config/scripts/edgeos-adblock -v
