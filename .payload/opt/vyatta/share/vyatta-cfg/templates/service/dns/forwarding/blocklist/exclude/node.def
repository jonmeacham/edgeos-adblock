multi:
type: txt
help: domains to GLOBALLY EXCLUDE from DNS forwarding domains and hosts blocklist

syntax:expression: pattern $VAR(@) "^[[:alnum:]][-.[:alnum:]]*[[:alnum:]]$"
                   ; "invalid domain name $VAR(@)"

